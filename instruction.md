我们刚把几辆车的历史数据从不同表格合进 May，里面有英里和公里混用、重复加油记录，还有一些行程只填了结束读数。现在报表里的距离、油耗和按里程提醒不太可信。我想先得到一份按车辆分组的校正预览，确认哪些记录会被调整，再一次性应用；操作中途出错或发现规则选错时，可以完整撤回这次批处理。原始录入内容要能追溯，之后新录入的数据也不能被旧的校正逻辑污染。

请把这套校正流程做成应用里可重复运行的功能，覆盖网页和 API 使用者，并让导出数据能说明哪些值是原始的、哪些是校正后的。不要影响没有参加校正的车辆和现有的手工维护流程。
